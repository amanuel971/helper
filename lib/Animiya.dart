import 'package:flutter/material.dart';

class Animiya extends StatelessWidget {
  const Animiya({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.blue,
        title: Text('Anemia'),
      ),
    );
  }
}
