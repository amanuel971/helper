import 'package:flutter/material.dart';

class BloodcountScreen extends StatelessWidget {
  const BloodcountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.yellow,
        title: Text('Blood count'),
      ),
    );
  }
}
