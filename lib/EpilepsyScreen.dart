import 'package:flutter/material.dart';

class EpilepsyScreen extends StatelessWidget {
  const EpilepsyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.red,
        title: Text('Epilepsy'),
      ),
    );
  }
}
