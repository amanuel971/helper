import 'package:flutter/material.dart';

class bloodScren extends StatelessWidget {
  const bloodScren({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.purple,
        title: Text('Bloodpresure'),
      ),
    );
  }
}
