import 'package:flutter/material.dart';

class heartScreen extends StatelessWidget {
  const heartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.green,
        title: Text('heart failuer'),
      ),
      body: MaterialApp(),
    );
  }
}
