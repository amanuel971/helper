import 'package:flutter/material.dart';

class suportScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('suport'),
      ),
      body: Center(
        child: Text('email : workuamanuel971@gmail.com'),
      ),
    );
  }
}
